// This is the same as Experience - you can merge them or keep separate
// For now, redirecting to experience section
export default function Work() {
  return null; // Experience section handles this
}
